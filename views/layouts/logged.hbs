<!DOCTYPE html>
<html lang="pt-br">

<head>
  {{>head}}
</head>

<body id="page-top">
  {{!-- Navbar --}}
  {{>nav}}
  <div id="wrapper">
    {{!-- Sidebar --}}
    {{>sidebar}}
    <div id="content-wrapper">

      {{{ body }}}

      {{!-- Footer --}}
      {{>footer}}
    </div>
    <!-- /.content-wrapper -->
  </div>
  <!-- /#wrapper -->

  <!-- Scroll to Top Button-->
  <a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
  </a>
  {{!-- Modal --}}
  {{>modal_logout}}
  {{!-- Modal Insert Transaction --}}
  {{>modal_insert_transaction}}
  {{!-- Scripts --}}
  {{>scripts}}
</body>

</html>